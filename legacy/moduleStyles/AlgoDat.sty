\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{AlgoDat}[2021/06/17]
\RequirePackage{templateSmall}

% If you found this template on the internet you probably have to comment this out
\RequirePackage{credentials}

\DeclareMathOperator{\Dt}{dt}
\DeclareMathOperator{\ft}{ft}


\newcommand{\dist}[1][]{\if\relax\detokenize{#1}\relax\texttt{dist}\else\texttt{dist[$#1$]}\fi}
\newcommand{\parent}[1][]{\if\relax\detokenize{#1}\relax\texttt{parent}\else\texttt{parent[$#1$]}\fi}

\newcommand{\weight}[2]{\texttt{weight($#1$,$#2$)}}
\newcommand{\Relax}[2]{\texttt{relax($#1$,$#2$)}}



\providecommand{\AlgoDat}{%
    \title{Algorithmen und Datenstrukturen Zusammenfassung}%
    \template@credentials@make{{emily}}
    %
%    \renewcommand{\thesection}{Exercise \arabic{section}.}%
%    \renewcommand\thesubsection{({\alph{subsection}})}%
%    \renewcommand\thesubsubsection{(\textit{\roman{subsubsection}})}%
}
